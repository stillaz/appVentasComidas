import { UdpCurrencyPipe } from './udp-currency.pipe';

describe('UdpCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new UdpCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
